<<<<<<< HEAD
name: Backend Server CI/CD

on:
  push:
    branches: [ "development", "main"]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff
        pip install -r requirements.txt

    - name: Proof Python code
      run: |
        pip install ruff
        ruff check ./main
=======
name: Backend Server CI/CD

on:
  push:
    branches: [ "development", "main"]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff
        pip install -r requirements.txt

    - name: Proof Python code
      run: |
        ruff check ./main.py
>>>>>>> 5846c121381d87d78286dc8d76ba76f70575490a
